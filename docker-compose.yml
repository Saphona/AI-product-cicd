services:
  app:
    image: YOUR_DOCKERHUB_USERNAME/ai-product-predictor:latest
    container_name: app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - 3000:3000

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
  #   depends_on:
  #     - app